## Used by docker-compose.yaml to run Postgres locally. Copy to `.env` (gitignored) to override
## the default local dev password; the compose file works out of the box without this file too.
POSTGRES_PASSWORD=postgres_dev_password
